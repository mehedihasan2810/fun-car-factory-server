export interface UserInput {
  name: string;
  email: string;
  role: string;
}

export interface CarInput {
  category: string;
  description: string;
  email: string;
  name: string;
  price: number;
  quantity: number;
  rating: number;
  sellerName: string;
  url: string;
}

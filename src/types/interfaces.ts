export interface UserInput {
  name: string;
  email: string;
  role: string;
}

export interface CarInput {
  category: string;
  description: string;
  email: string;
  name: string;
  price: string;
  quantity: string;
  rating: string;
  sellerName: string;
  url: string;
}
